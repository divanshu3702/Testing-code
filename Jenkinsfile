pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Code Checked Out'
            }
        }

        stage('Build') {
            steps {
                echo 'Build Successful 🚀'
            }
        }
    }
}
